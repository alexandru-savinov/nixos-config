Fleet Matrix architecture simplification — collapse the duplicated NixOS module/flake/serve/MCP/doc surface of this flake into shared helpers and a clean host layout, proving each change behavior-preserving (or intended) by `nix eval` before any deploy. Epic https://github.com/alexandru-savinov/nixos-config/issues/495.

## Context

- **Epic:** https://github.com/alexandru-savinov/nixos-config/issues/495 (9 verified findings; this plan executes 8 of them — see "Operator-only follow-up" for the 9th, #493, which is deliberately excluded from the autonomous run).
- **Repo:** `/Users/alexandru/nixos-config`. **Branch from `main`** (PRs target `main`; it is protected — open a PR, never push to main). Prefer `--worktree` so the run is isolated. The `docs/architecture-*.{html,json,md}` files on `fix/epic-454-round4` are UNTRACKED audit artifacts — never move, commit, or delete them (a `main`-based worktree won't even contain them).
- **Host running ralphex:** macOS (darwin), Determinate Nix 2.33. `nix eval .#nixosConfigurations.<host>.config.<path> --json` WORKS cross-platform (pure eval) and is the PRIMARY verification tool. Use `--impure --expr 'let f = builtins.getFlake (toString ./.); in ...'` for `_module.specialArgs` reads. `timeout` is unavailable on macOS (use `gtimeout` or omit).
- **What this Mac CANNOT do:** build x86_64-linux / aarch64-linux derivations, boot nixosTest VMs, or `nixos-rebuild`. Those gates belong to **CI (the PR's `nix flake check --all-systems`)** or to the operator on the Pi/remote host — never Mac-local.
- **Hosts:** `rpi5`, `rpi5-full` (aarch64) ; `sancta-choir`, `sancta-claw`, `hermes-claw`, `zero-kuzea` (x86_64-linux).
- **Autonomy classes:** Tasks 1–5, 7, 8 are BUILD-GATED (edit + `nix eval` checks run autonomously on Mac; the authoritative gate is `nix flake check` in CI). **Task 6 (#489) is DEPLOY-GATED:** ralphex makes the eval-proven-equivalent edit ONLY; the rollout to the remote VPS is HUMAN-ONLY. NEVER let ralphex `nixos-rebuild switch`/`boot` a remote host.
- **PR #441 coexistence:** an open, unmerged PR (`feat/sancta-port-openrouter`) edits `tests/module-eval.nix`, `secrets/secrets.nix`, `open-webui.nix`, and the choir/claw/rpi5-full host configs. There is **no logical conflict** with the 8 tasks here — only minor rebase friction on those shared files. If #441 merges mid-run, rebase the epic branch. (The one task that *did* entangle with #441, #493, is excluded — see the appendix.)

### Verify-first mandate (applies to EVERY task)

Before doing any work on a task, run its named **verify-first** (BEFORE) check and confirm the baseline matches the finding. Do the minimal change. Then run the **closing check** and paste the output / exit code / store paths as evidence in the commit. Never report a task done on "looks done" or on "it evals/parses" alone — for behavior-preserving refactors the closing check is a rendered-artifact / store-path / attrName comparison that proves *behavior*, not parsing. Do not weaken or edit a check to make it pass; fix the root cause. If a check refutes the baseline (finding already done / drifted), STOP and report rather than forcing the change.

### CI tripwire (the single most likely failure mode across this batch)

`tests/module-eval.nix` is a WIRED check (`flake.nix` `checks.x86_64-linux.module-eval`, ~line 302). It imports `modules/services/qdrant.nix` 3× (~95/105/116), `modules/services/openclaw.nix` 3× (~413/430/450), `modules/services/claude.nix` 2 cases (~587–603), and `modules/services/unifi-mcp.nix` 2 cases (~566–584). **#491 MUST remove its 3 qdrant cases** when deleting qdrant.nix; **#486 MUST remove its 2 claude cases** when deleting claude.nix; **#488 MUST keep unifi-mcp.nix importable standalone.** A dangling import of a deleted file reddens CI.

## Tasks

### Task 1: #492 — archive stale root migration .md + historical docs/ plans into docs/plans/completed/ and fold root completed/ in

- [ ] verify-first: run `git grep -n -F -e MIGRATION-RESUME.md -e openrouter-vkeys-c-migration.md -e plan-open-webui-sancta-choir.md -e kuzea-plan.md -e nixframe-plan.md -e OPENCLAW-MICROVM-PLAN.md -e TELEGRAM-OPENCLAW-SETUP.md -e plan-home-assistant.md -- . ':!docs/architecture-map.html' ':!docs/architecture-graph.json' ':!docs/architecture-analysis.md'` and confirm baseline: the orphan docs have only self-references, and `plan-home-assistant.md` is referenced by basename in exactly 3 tracked .nix comments (`secrets/secrets.nix:127`, `hosts/rpi5-full/configuration.nix:327`, `modules/services/home-assistant-mcp-claude.nix:22`).
- [ ] `git mv` the 3 root migration files (`MIGRATION-RESUME.md`, `openrouter-vkeys-c-migration.md`, `plan-open-webui-sancta-choir.md`) into `docs/plans/completed/`.
- [ ] `git mv` the 4 historical docs (`docs/kuzea-plan.md`, `docs/nixframe-plan.md`, `docs/OPENCLAW-MICROVM-PLAN.md`, `docs/TELEGRAM-OPENCLAW-SETUP.md`) into `docs/plans/completed/`.
- [ ] `git mv completed/plan-home-assistant.md docs/plans/completed/` and remove the now-empty root `completed/` directory.
- [ ] Update the 3 .nix comment pointers to read `docs/plans/completed/plan-home-assistant.md` — comment text ONLY; do NOT touch publicKeys/recipients lines in `secrets/secrets.nix`.
- [ ] Run `nix fmt` (the 3 edited .nix files must pass the formatter).
- [ ] closing check: run `test ! -e completed && test ! -e MIGRATION-RESUME.md && test ! -e openrouter-vkeys-c-migration.md && test ! -e plan-open-webui-sancta-choir.md && test ! -e docs/kuzea-plan.md && test ! -e docs/nixframe-plan.md && test ! -e docs/OPENCLAW-MICROVM-PLAN.md && test ! -e docs/TELEGRAM-OPENCLAW-SETUP.md` AND `git grep -n 'plan-home-assistant.md' -- '*.nix'` (all 3 lines now point at `docs/plans/completed/`) AND `git grep -nF -e MIGRATION-RESUME.md -e kuzea-plan.md -e nixframe-plan.md -e OPENCLAW-MICROVM-PLAN.md -e TELEGRAM-OPENCLAW-SETUP.md -e openrouter-vkeys-c-migration.md -e plan-open-webui-sancta-choir.md -- '*.nix' '*.sh' '.github/' flake.nix` (returns nothing) AND `nix eval .#nixosConfigurations.rpi5-full.config.system.build.toplevel.drvPath` (still evaluates). PASS = root has no `completed/` dir and none of the 7 named docs; `docs/plans/completed/` holds all 8 archived files plus the 2 pre-existing ones; the 3 .nix comments resolve; rpi5-full toplevel still evaluates.
- [ ] update epic: check off #492 in epic #495 / note the closing-check evidence in the commit.

### Task 2: #491 — delete dormant RAG-on-ARM stack (qdrant.nix + open-webui-arm-fix.nix + qdrant-integration.nix + 3 module-eval qdrant cases + commented rpi5-full blocks)

- [ ] verify-first: run `grep -rn 'qdrant' --include=*.nix . | grep -v '.ralphex'` and `nix eval .#checks.x86_64-linux.module-eval` and confirm baseline: `tests/module-eval.nix` imports `../modules/services/qdrant.nix` at lines ~95/105/116, no uncommented host enables `services.qdrant-tailscale`, `nix eval .#nixosConfigurations.sancta-choir.config.services.open-webui-tailscale.vectorDb.type` is `"chromadb"`, and `module-eval` resolves to a derivation.
- [ ] `git rm` `modules/services/qdrant.nix`, `modules/system/open-webui-arm-fix.nix` (NOTE: under `modules/system/`, not `modules/services/`), and `tests/qdrant-integration.nix`.
- [ ] Remove the "Qdrant" section from `tests/module-eval.nix` — the section header plus the 3 cases (qdrant-minimal / qdrant-on-disk / qdrant-disabled, lines ~92–119).
- [ ] In `hosts/rpi5-full/configuration.nix` strip the commented qdrant/open-webui-arm-fix import block (~51–54), the commented `services.qdrant-tailscale` block (~228–231), and the commented Gatus `rpi5-qdrant` endpoint (~272). Do NOT touch `modules/services/open-webui.nix` or its inert vectorDb=qdrant machinery (it is LIVE on sancta-choir).
- [ ] closing check: run `grep -rn 'qdrant' --include=*.nix . | grep -v '.ralphex'` (no `qdrant.nix` import refs, no module-eval qdrant cases, no commented qdrant blocks in rpi5-full; the only acceptable residual hits are the out-of-scope `open-webui.nix` vectorDb machinery) AND `nix eval .#checks.x86_64-linux.module-eval` (still a derivation) AND `nix eval .#nixosConfigurations.sancta-choir.config.system.build.toplevel.drvPath` and `nix eval .#nixosConfigurations.rpi5-full.config.system.build.toplevel.drvPath` (both still evaluate). PASS = grep finds no remaining qdrant.nix import / module-eval qdrant cases / commented qdrant blocks; module-eval still a derivation; choir + rpi5-full toplevel still evaluate; `nix flake check` green in CI. Note in the commit that the aarch64 RAG overlay is intentionally dropped from main (restorable from history).
- [ ] update epic: check off #491 in epic #495 / note the closing-check evidence in the commit.

### Task 3: #490 — wire n8n-declarative + open-webui-tavily into checks.x86_64-linux on pinned pkgs; drop qdrant-integration

- [ ] verify-first: run `nix eval .#checks.x86_64-linux --apply 'builtins.attrNames' --json` and confirm baseline `["module-eval","openclaw-zdr-proxy"]`; confirm `head -1 tests/n8n-declarative.nix tests/open-webui-tavily.nix` both show `{ pkgs ? import <nixpkgs> { } }:`. (If Task 2 already deleted `tests/qdrant-integration.nix`, the delete step below is a no-op — verify with `test -e tests/qdrant-integration.nix`.)
- [ ] In `flake.nix` `checks.x86_64-linux` (block ~294–310; `pkgs = nixpkgsFor.x86_64-linux` already carries `allowUnfree=true`), add next to `openclaw-zdr-proxy`: `n8n-declarative = pkgs.testers.nixosTest (import ./tests/n8n-declarative.nix { inherit pkgs; });` and `open-webui-tavily = pkgs.testers.nixosTest (import ./tests/open-webui-tavily.nix { inherit pkgs; });`.
- [ ] In `tests/n8n-declarative.nix:1` and `tests/open-webui-tavily.nix:1`, drop the `? import <nixpkgs> { }` default so the signature is `{ pkgs }:` (mirror `openclaw-zdr-proxy.nix`). Do NOT keep the channel fallback (open-webui is unfree; bare `import <nixpkgs> {}` lacks allowUnfree). Keep them under `checks.x86_64-linux` only — not aarch64 / forAllSystems.
- [ ] If still present, `git rm tests/qdrant-integration.nix` (wired into nothing — plain delete is safe). Do NOT touch `tests/module-eval.nix`'s qdrant cases (that is Task 2).
- [ ] closing check: run `nix eval .#checks.x86_64-linux --apply 'cs: builtins.sort (a: b: a < b) (builtins.attrNames cs)' --json` AND `test ! -e tests/qdrant-integration.nix && echo DELETED`. PASS = attrNames equals `["module-eval","n8n-declarative","open-webui-tavily","openclaw-zdr-proxy"]` (both new entries are derivations), `tests/qdrant-integration.nix` is gone, repo-wide grep returns nothing. The VMs-actually-boot proof is the PR's `nix flake check --all-systems` on a Linux builder (cannot boot x86_64 VMs on this Mac) — expect real CI runtime, and possibly a fix if the long-unrun VM tests are broken.
- [ ] update epic: check off #490 in epic #495 / note the closing-check evidence in the commit.

### Task 4: #487 — extract lib/mkTailscaleServe.nix for the copy-pasted tailscale-serve oneshots; reconcile grep pattern

- [ ] verify-first: run `grep -rn 'tailscale serve status' modules/services/ hosts/ | sort` and `... | wc -l` and confirm baseline: 9 hand-written guards across 9 files (8 modules + `hosts/sancta-claw/openclaw-tailscale-serve.nix:58` hardcoded port 18789); 6 use literal `grep -q "https:PORT"`, gatus + home-assistant use regex `grep -qE "https?://.*:PORT( |$)"`. Capture the BEFORE rendered units: `nix eval --raw .#nixosConfigurations.rpi5-full.config.systemd.services.tailscale-serve-n8n.script` (and `.preStop`, `.serviceConfig.TimeoutStartSec`) for each migrated service.
- [ ] Create `lib/mkTailscaleServe.nix` taking `{ pkgs, name, description, port, httpsPort, requires ? [], partOf ? [], host ? "127.0.0.1", timeoutStartSec ? 150, ... }`, emitting the standard oneshot (Type=oneshot, RemainAfterExit, tailscaled-wait + port-wait loop, ONE reconciled grep guard pattern, `serve --bg --https`, preStop `serve --https off`). Base it on the `wait_for()` form already factored in qdrant/nullclaw.
- [ ] Replace the serve unit in the 8 modules (`n8n.nix`, `open-webui.nix`, `gatus.nix`, `qdrant.nix` — **SKIP if Task 2 deleted it**, `unifi-mcp.nix` nested in a listToAttrs block, `nullclaw.nix`, `home-assistant.nix`, `openclaw.nix`) plus `hosts/sancta-claw/openclaw-tailscale-serve.nix:58` with a single helper call each. Pass per-service timeouts through (home-assistant uses 300s/180s — do NOT flatten to 150s). **Preserve openclaw's placeholder no-op** (it only echoes "port reserved", never calls `serve --bg`) — never turn it into a real serve. Do NOT change unit NAMES. Do NOT absorb the companion `systemd.services.<main>.wants = [serve]` lines (the #449 start-gap closer).
- [ ] closing check: re-run for each migrated service `nix eval --raw .#nixosConfigurations.<host>.config.systemd.services.<unit>.script` (and `.preStop`, `.serviceConfig.TimeoutStartSec`) and diff against the captured BEFORE — must be semantically equal except the intentionally-unified grep pattern; AND `grep -rn 'tailscale serve status' modules/services/ hosts/ | grep -v mkTailscaleServe` drops toward 0 inline guards. PASS = all previously-rendering serve units still eval; `.script`/`.preStop` unchanged except the unified grep; literal-vs-regex split gone; `nix flake check` (module-eval imports qdrant.nix/openclaw.nix) green in CI. If the eval-diff shows ANY unintended change to serve/preStop on a live host (sancta-choir/sancta-claw/zero-kuzea), STOP and escalate to a human deploy review.
- [ ] update epic: check off #487 in epic #495 / note the rendered-unit diff evidence in the commit.

### Task 5: #488 — factor the triplicated ~/.claude.json MCP-merge flock oneshot into lib/mkClaudeMcpOneshot.nix

- [ ] verify-first: capture the 4 baseline ExecStart store paths BEFORE editing: `nix eval --raw .#nixosConfigurations.rpi5.config.systemd.services."unifi-mcp-config-nixos".serviceConfig.ExecStart` ; `...rpi5...n8n-mcp-config-nixos...` ; `...rpi5-full...n8n-mcp-config-nixos...` ; `...rpi5-full...home-assistant-mcp-config-nixos...`. Record the 4 store hashes.
- [ ] Create `lib/mkClaudeMcpOneshot.nix` emitting the shared `writeShellScript` body parameterized by `{ pkgs, name, mcpConfig, users, injectStep }` — `injectStep` is a per-module shell snippet operating on the `<NAME>_MCP_CONFIG` var.
- [ ] Reduce `modules/services/n8n-mcp-claude.nix` (`generateMcpConfigScript`, ~87–138) to a thin caller with nullable `apiKeyFile` injection.
- [ ] Reduce `modules/services/home-assistant-mcp-claude.nix` (~55–102) to a thin caller with nullable `tokenFile` injection.
- [ ] Reduce `modules/services/unifi-mcp.nix` (~159–236) to a thin caller with MANDATORY `passwordFile` injection (fail-loud) AND the `useDocker` `.args`-append vs `.env` branch. **PRESERVE the load-bearing heredoc indentation at unifi-mcp.nix:164–169** (4-space indent so `''` stripping lands `MCPEOF` at column 0). Do NOT touch `copilot.nix`. Do NOT change configured users, MCP entry names (`n8n-mcp`/`home-assistant`/`unifi`), file modes (chmod 600), chown, or flock semantics.
- [ ] closing check: re-run the same 4 `nix eval --raw ... ExecStart` commands and assert each store path is UNCHANGED (byte-identical = provably behavior-preserving). If a path changed, diff old vs new script and confirm the change is purely cosmetic whitespace. PASS = all 4 ExecStart store paths identical to baseline; the 2 unifi-mcp module-eval cases (~566–584) still evaluate; the flock/RMW/mktemp/mv skeleton now lives once in `lib/mkClaudeMcpOneshot.nix`; module-eval green in CI. Do NOT claim done on "evals" alone — the closing check is store-path-unchanged.
- [ ] update epic: check off #488 in epic #495 / note the 4 store-path equalities in the commit.

### Task 6: #489 — DEPLOY-GATED de-dup of sancta-choir SSH hardening + networking (host.nix vs networking.nix)

> **HUMAN-GATED — DO NOT DEPLOY.** This rewrites the LIVE eth0 static IP / defaultGateway / nameservers and the SSH access policy of the remote headless Hetzner VPS `sancta-choir`. A wrong value or SSH regression on switch can lock the operator out (rescue-mode recovery, #252-class hazard). ralphex makes the eval-proven-equivalent edit ONLY. The actual rollout (`nixos-rebuild boot` + reboot, prefer boot over switch, Hetzner rescue/console ready) is the operator's job. NEVER `nixos-rebuild switch` this remote host.

- [ ] verify-first: run `nix eval .#nixosConfigurations.sancta-choir.config.networking.nameservers --json` and `... .networking.interfaces.eth0.ipv4.addresses --json` and `grep -c 'ssh-hardened' hosts/sancta-choir/configuration.nix` and confirm baseline: nameservers `["8.8.8.8","185.12.64.1","185.12.64.2","185.12.64.1","185.12.64.2"]` (185.x duplicated), eth0 has `116.203.223.113/32` TWICE, sancta-choir does NOT import ssh-hardened (grep = 0).
- [ ] In `hosts/sancta-choir/configuration.nix` (imports list ~37–47) add `../../modules/system/ssh-hardened.nix`.
- [ ] In `modules/system/host.nix` remove the inline `services.openssh` block (~28–35) and the networking eth0/gateway/nameservers/useDHCP/localCommands (~7–26), keeping `modules/system/networking.nix` as the single networking source (it has the superset nameservers `[8.8.8.8 185.12.64.1 185.12.64.2]`, IPv6 fe80::, udev MAC rule, ipv4.routes — these MUST survive). Keep allowUnfree, hostName mkForce, firewall, timezone, locale, root authorizedKeys (relocate if host.nix ends up empty, then drop the import). Before removing host.nix's `localCommands` route, confirm `networking.nix` ipv4.routes already covers the Hetzner gateway.
- [ ] Run `nix fmt`.
- [ ] closing check (eval-proven equivalence only — NO deploy): `nix eval .#nixosConfigurations.sancta-choir.config.networking.nameservers --json` (expect `["8.8.8.8","185.12.64.1","185.12.64.2"]`, NO repeats) AND `... .interfaces.eth0.ipv4.addresses --json` (exactly one `116.203.223.113/32`) AND `... .services.openssh.settings.PermitRootLogin --json` (`"prohibit-password"`) AND `... .services.openssh.settings.PasswordAuthentication --json` (`false`) AND `... .networking.defaultGateway --json` (`172.31.1.1`) AND `nix eval .#nixosConfigurations.sancta-choir.config.system.build.toplevel.drvPath` (evaluates). PASS = nameservers deduped to the superset, single eth0 IP, SSH policy + gateway unchanged, toplevel still evaluates. CI `nix flake check` is the build gate.
- [ ] HUMAN-GATED: do NOT deploy — leave `nixos-rebuild boot` + reboot of the remote VPS to the operator (Hetzner rescue/console ready).
- [ ] update epic: check off #489 in epic #495 / note the eval evidence in the commit AND flag in the PR body that an operator deploy (boot+reboot) is required.

### Task 7: #486 — consolidate legacy claude.nix into claude-shared (headless); migrate sancta-choir + sancta-claw with users=["root"]; drop their claude-code specialArg

- [ ] verify-first: run `nix eval --json '.#nixosConfigurations.sancta-choir.config.environment.systemPackages' | grep -o 'claude-code-[0-9.]*'` (BEFORE: `claude-code-2.1.81`) and `nix eval --json '.#nixosConfigurations.rpi5.config.home-manager.users.nixos.home.packages' | grep -o 'claude-code-[0-9.]*'` (`claude-code-2.1.150`) to prove the two-source version skew; and `nix eval '.#nixosConfigurations.sancta-choir.config.home-manager.users' --apply builtins.attrNames` (and sancta-claw) to confirm HM runs ONLY for `["root"]` (no nixos user). Confirm `grep -n 'customModules.claude.enable' hosts/sancta-choir/configuration.nix hosts/sancta-claw/configuration.nix` shows both present.
- [ ] In `hosts/sancta-choir/configuration.nix`: swap import `../../modules/services/claude.nix` → `../../modules/services/claude-shared.nix` (~44) and replace `customModules.claude.enable = true` (~51) with `customModules.claudeShared = { enable = true; users = [ "root" ]; }`.
- [ ] In `hosts/sancta-claw/configuration.nix`: same import swap (~22) and same `customModules.claudeShared` with `users = [ "root" ]` (~30).
- [ ] In `flake.nix`: drop `claude-code` and add `claude-shared` in the sancta-choir specialArgs (~134) and sancta-claw specialArgs (~151). **KEEP the `claude-code` flake INPUT** (~39/69) and its rpi5/rpi5-full wiring (~200/221) and the claude-shared input wiring for the Pis — still needed (openclaw.nix module-eval fixtures + the Pis).
- [ ] Verify the intermediate state before deleting anything (rewire FIRST, delete LAST): `nix eval --json '.#nixosConfigurations.sancta-choir.config.home-manager.users.root.home.packages' | grep -o 'claude-code-[0-9.]*'` should now show `claude-code-2.1.150`.
- [ ] `git rm modules/services/claude.nix` and remove the `claude-missing-input` + `claude-disabled` cases from `tests/module-eval.nix` (~587–603) that import it. Do NOT remove openclaw-related claude-code module-eval fixtures.
- [ ] Run `nix fmt`.
- [ ] closing check: (a) `nix eval '.#nixosConfigurations.sancta-choir.config.home-manager.users' --apply builtins.attrNames` (still includes `"root"`); (b) `nix eval --json '.#nixosConfigurations.sancta-choir.config.home-manager.users.root.home.packages' | grep -o 'claude-code-[0-9.]*'` (equals `2.1.150`, skew gone); (c) `grep -rn 'customModules.claude\b\|services/claude.nix' hosts/ tests/ modules/` (returns NOTHING); (d) `test ! -e modules/services/claude.nix && echo GONE`. PASS = choir + claw resolve the same claude-code version as rpi5 (2.1.150, not 2.1.81); claude.nix gone; no `customModules.claude`/`services/claude.nix` refs remain; choir HM still has root; module-eval no longer imports claude.nix; `nix flake check` green in CI. Note the 2.1.81→2.1.150 bump in the PR. If eval/build shows the HM closure bloats the small VPS unacceptably, implement the issue's package-only enable flag rather than abandoning the migration.
- [ ] update epic: check off #486 in epic #495 / note the version-flip + grep evidence in the commit.

### Task 8: #494 — add mkRpiHost/mkX86Host host-builders; fix zero-kuzea inline unstable import (allowUnfree); fold agenix wiring

- [ ] verify-first: run `nix eval --impure --expr 'let f = builtins.getFlake (toString ./.); in { zk = f.nixosConfigurations.zero-kuzea._module.specialArgs.pkgs-unstable.config.allowUnfree; choir = f.nixosConfigurations.sancta-choir._module.specialArgs.pkgs-unstable.config.allowUnfree; }' --json` and confirm baseline `{"choir":true,"zk":false}`; confirm `grep -n agenixModule flake.nix` shows it appended to all 6 host module lists (~141/158/174/190/209/230).
- [ ] In `flake.nix`: change zero-kuzea specialArgs (~183) from inline `import nixpkgs-unstable { system = "x86_64-linux"; }` to `pkgs-unstable = pkgs-unstable-x86;` (the smallest, highest-value slice — do this first). Do NOT rename the `pkgs-unstable` key; do NOT set allowUnfree=false anywhere.
- [ ] Add `mkRpiHost` (param: configPath) and `mkX86Host` helpers in the let-block (~70–96) to collapse the identical rpi5 (~198–211) / rpi5-full (~219–232) wirings and the 4 x86 wirings — reproducing the FULL per-host module list, parameterized only on the configuration.nix path. Do NOT drop any per-host module (raspberry-pi-5.base, vscode-server, home-manager, etc.). Do NOT add a second `../common.nix` import to rpi5-full (it imports it transitively via `../rpi5/configuration.nix:49`).
- [ ] Optionally fold `agenixModule` + `agenix.nixosModules.default` into a PRIVATE let-block helper list (e.g. `agenixHostModules = [ agenix.nixosModules.default agenixModule ];`) referenced by both builders. Do NOT fold them bare into `hosts/common.nix` (it is a plain `{ pkgs, lib, ... }` module exported as `nixosModules.common`; coupling it to the agenix input breaks that export). Do NOT delete the agenix input or `agenix.nixosModules.default`.
- [ ] Run `nix fmt`.
- [ ] closing check: `nix eval --impure --expr 'let f = builtins.getFlake (toString ./.); in builtins.all (h: f.nixosConfigurations.${h}._module.specialArgs.pkgs-unstable.config.allowUnfree) ["zero-kuzea" "sancta-choir" "sancta-claw" "hermes-claw"]' --json` (must print `true`) AND `nix eval .#nixosConfigurations.<h>.config.system.build.toplevel.drvPath` for ALL SIX hosts (each still resolves) AND `grep -c agenixModule flake.nix` (dropped if deduped). PASS = zero-kuzea allowUnfree flips false→true; every host's toplevel.drvPath still evaluates (no host lost a module); if agenix deduped, the literal no longer appears 6×; `nix flake check` / module-eval green in CI.
- [ ] update epic: check off #494 in epic #495 / note the allowUnfree-flip + all-six-drvPath evidence in the commit.

## Constraints

Things the agent must NOT do:

- **main is protected.** Branch off `main` and open a PR — do NOT push to main.
- **NEVER run `nixos-rebuild switch`/`boot` against any remote VPS** (sancta-choir, sancta-claw, hermes-claw, zero-kuzea). Task 6 (#489) is DEPLOY-GATED: ralphex stops at the eval-proven edit; the operator does the rollout.
- **CI tripwire:** NEVER delete `qdrant.nix` without removing its 3 module-eval cases (Task 2); NEVER delete `claude.nix` without removing its 2 module-eval cases (Task 7); KEEP `unifi-mcp.nix` importable standalone (Task 5).
- **NEVER remove the `claude-code` flake INPUT** (flake.nix ~39/69). The Pis (~200/221) and `openclaw.nix` (module-eval fixtures) still consume it. Task 7 only drops it from choir(:134)/claw(:151) specialArgs.
- **Task 7 must use `users = [ "root" ]`**, not claude-shared's default `["nixos"]` — choir/claw run home-manager only for root and have NO nixos user.
- **Task 4 (#487):** do NOT turn openclaw.nix's placeholder serve into a real serve; do NOT flatten home-assistant's 300s/180s timeouts; do NOT change systemd unit NAMES (renaming orphans the running unit on a live host); do NOT absorb the companion `<main>.wants = [serve]` lines; do NOT forget the 9th instance `hosts/sancta-claw/openclaw-tailscale-serve.nix:58`.
- **Task 5 (#488):** do NOT touch `copilot.nix`; PRESERVE the unifi-mcp heredoc indentation (164–169); the helper MUST take a secret-injection hook (a bare `{name,mcpConfig,users}` signature silently drops unifi's docker branch).
- **Task 2 (#491):** do NOT touch `modules/services/open-webui.nix` (live on sancta-choir; its inert vectorDb=qdrant strings are intentionally out of scope — the closing-check grep must EXPECT those residual hits, not treat them as failure). The arm-fix file is `modules/system/open-webui-arm-fix.nix`.
- **Task 1 (#492):** use `git mv` (not plain mv). Do NOT move `docs/plans/2026-05-13-anki-cache.md` (active), `docs/ARM-COMPATIBILITY.md`, `docs/DISASTER-RECOVERY.md`, `docs/OFFICIAL-OPENCLAW-TELEGRAM.md`, `README.md`, `SECRETS-ROTATION.md`, or `CLAUDE.md`. Edit ONLY the comment on `secrets/secrets.nix:127` — NEVER the publicKeys/recipients lines. Do NOT touch `.ralphex/` or the untracked `docs/architecture-*` artifacts.
- **Task 8 (#494):** do NOT fold agenix bare into `hosts/common.nix` (use a private let-block helper list); do NOT delete the agenix input; do NOT rename the `pkgs-unstable` key; do NOT set allowUnfree=false anywhere; do NOT drop any per-host module; do NOT add a second `../common.nix` import to rpi5-full.
- **Verification gate:** use `nix eval` / `nix flake check` (eval pieces) on this Mac as the autonomous gate. Building x86_64/aarch64-linux derivations and nixosTest VM boots do NOT run on darwin — leave them to CI (the PR) or the Pi/remote host. `timeout` is unavailable on macOS.
- **Behavior-preserving refactors (Tasks 4 & 5):** never report done on "it evals/parses." The closing check is a rendered-unit / store-path comparison proving the emitted script is unchanged. If a refactor shows an unintended change on a live host, STOP and escalate.
- **Commit per task** with the closing-check evidence (output / exit code / store paths) in the message, and check off the issue in epic #495.

---

## Operator-only follow-up (NOT run by ralphex)

> The 9th finding, **#493**, is intentionally **excluded from the autonomous run** because it entangles with open PRs **#441** (`feat/sancta-port-openrouter`, two-tier openrouter — rewrites `secrets/secrets.nix` + `hermes-service.nix`, and shadows hermes-env's OPENROUTER_API_KEY via a new `sancta-port-openrouter-dev.age`) and **#435** (also rewrites `hermes-service.nix`). It is DEPLOY-GATED on the non-atomic GRUB VPS `hermes-claw`. Do these steps by hand, in order, AFTER #441 + #435 land. This section uses numbered steps (no `- [ ]`) on purpose so ralphex never executes it.

1. **Resolve #441 + #435 first.** #441 has been blocked ~3 weeks on you encrypting `secrets/sancta-port-openrouter-prd.age` (raw) + `secrets/sancta-port-openrouter-dev.age` (env-file). Merge or close both, then rebase. Post-#441, hermes already reads `OPENROUTER_API_KEY` from `sancta-port-openrouter-dev.age` (EnvironmentFile last-wins), so hermes-env's OR line is already shadowed — **#493 simplifies to "remove the now-dead OR line / retire hermes-env.age", not "rebuild env from openrouter-api-key.age" (which #441 is retiring anyway).**
2. verify-first: `for f in hermes-env openrouter-api-key zero-kuzea-telegram-bot-token; do printf '%-32s stanzas=%s\n' "$f" "$(grep -a -c '^-> ' secrets/$f.age)"; done` (hermes-env = 5 on-disk vs 3 declared) and `bash scripts/check-secret-recipients.sh secrets 2>&1 | grep -i hermes` (KNOWN_PENDING_DRIFT WARN fires, #448).
3. Compose hermes's env at activation from the two RAW source secrets (both already decryptable on hermes-claw) into `/run/hermes/hermes.env` (tmpfs, 0400, owner=hermes, `KEY=VALUE` form — do NOT symlink a raw value file), point `hermes-service.nix` `environmentFiles` at it, and remove `age.secrets.hermes-env`. Drop `hermes-env.age` from `secrets/secrets.nix`, `git rm secrets/hermes-env.age`, AND remove its KNOWN_PENDING_DRIFT entry from `scripts/check-secret-recipients.sh` — all three together (leaving the .age on disk fails the recipient check).
4. **Do NOT touch the refuted secrets:** tavily-api-key vs kuzea-tavily, caldav-credentials vs kuzea-caldav, and the 3 distinct telegram bot tokens are deliberate least-privilege / different bots — NOT consolidation candidates. Do NOT broaden any recipient set.
5. eval closing check (Mac): `nix eval --impure --json --expr 'let s = import ./secrets/secrets.nix; in builtins.hasAttr "hermes-env.age" s'` (false), `grep -c 'hermes-env.age' scripts/check-secret-recipients.sh` (0), `ls secrets/hermes-env.age` (gone), `bash scripts/check-secret-recipients.sh secrets` (exit 0, no hermes-env line), `nix eval .#nixosConfigurations.hermes-claw.config.system.build.toplevel.drvPath` (evaluates).
6. **Runtime closing check (on-host, operator):** deploy via `nixos-rebuild boot` + reboot, then confirm `/run/hermes/hermes.env` exists 0400 owner=hermes with BOTH keys and `systemctl is-active hermes-agent` before declaring success. This closes #448.
